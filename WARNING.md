# ⚠️ Критические предупреждения и известные проблемы

> **Последнее обновление:** 29 декабря 2025  
> **Версия проекта:** Uroborous Ultimate AI v1.x

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (требуют немедленного внимания)

### 1. Отсутствует Rate Limiting для LLM провайдеров

**Риск:** 💸 Финансовый / 🔒 Безопасность

При ошибке в коде или DDoS-атаке система может отправить тысячи запросов к API, что приведёт к:
- Неконтролируемым расходам ($100+ за минуты)
- Блокировке аккаунтов OpenAI/Anthropic
- Исчерпанию лимитов

**Затронутые файлы:**
- `backend/llm/openai_provider.py`
- `backend/llm/anthropic_provider.py`
- `backend/llm/ollama_provider.py`

**Временное решение:**
```python
# Добавить в LLMProviderManager
from backend.core.rate_limiter import RateLimiter

rate_limiter = RateLimiter(
    requests_per_minute=60,
    tokens_per_minute=100000
)
```

**Статус:** 🔴 Не реализовано

---

### 2. Few-shot примеры НЕ используются в промптах

**Риск:** 📉 Качество ответов на 15-20% ниже потенциального

`LearningSystem` сохраняет успешные решения в SQLite, но агенты **не загружают их в промпты**.

**Проблема в файле:** `backend/agents/base.py`

```python
# СЕЙЧАС: Промпт строится без примеров
system_prompt = self._build_system_prompt(task)

# НУЖНО: Добавить примеры из learning system
examples = await self._learning_system.get_successful_examples(
    agent_name=self.name, task_type=task_type, limit=2
)
system_prompt = self._build_system_prompt(task, examples=examples)
```

**Статус:** 🔴 Не реализовано

---

### 3. Потенциальная утечка памяти в кэшировании

**Риск:** 💾 Out of Memory при длительной работе

**Затронутые файлы:**
- `backend/core/advanced_cache.py` — нет maxsize
- `backend/rag/cache.py` — нет TTL
- `backend/llm/providers.py` — response cache без лимита

**Симптомы:**
- Постепенный рост использования RAM
- Замедление после нескольких часов работы
- OOM kill при длительных сессиях

**Решение:** Добавить `TTLCache` с ограничениями:
```python
from cachetools import TTLCache
cache = TTLCache(maxsize=1000, ttl=3600)  # 1000 записей, 1 час
```

**Статус:** 🔴 Не реализовано

---

### 4. История чата ограничена 10 сообщениями

**Риск:** 🧠 Модель "забывает" контекст в длинных разговорах

**Файл:** `backend/api/routers/chat.py`

При разговоре длиннее 10 сообщений модель теряет начальный контекст. Пользователь должен повторять информацию.

**Решение:** Суммирование старых сообщений через LLM:
```python
if len(messages) > 10:
    old_messages = messages[:-10]
    summary = await summarize_messages(old_messages)
    messages = [{"role": "system", "content": f"Summary: {summary}"}] + messages[-10:]
```

**Статус:** 🔴 Не реализовано

---

## 🟠 ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ (исправить в ближайшее время)

### 5. Нет валидации синтаксиса генерируемого кода

**Риск:** 🐛 CodeWriterAgent может выдать синтаксически некорректный код

**Файл:** `backend/agents/code_writer.py`

Код генерируется без проверки через AST. Ошибки обнаруживаются только при выполнении.

**Решение:**
```python
import ast

def validate_python_code(code: str) -> tuple[bool, str]:
    try:
        ast.parse(code)
        return True, ""
    except SyntaxError as e:
        return False, f"Syntax error at line {e.lineno}: {e.msg}"
```

**Статус:** 🟠 Не реализовано

---

### 6. Отсутствует Correlation ID для логов

**Риск:** 🔍 Невозможно отследить запрос через все компоненты

При отладке проблемы приходится вручную сопоставлять логи разных компонентов по timestamp.

**Затронуто:** Все компоненты системы

**Решение:**
```python
import uuid
from contextvars import ContextVar

correlation_id: ContextVar[str] = ContextVar('correlation_id', default='')

# В middleware:
@app.middleware("http")
async def add_correlation_id(request, call_next):
    correlation_id.set(str(uuid.uuid4()))
    response = await call_next(request)
    return response
```

**Статус:** 🟠 Не реализовано

---

### 7. WebSocket не используется для прогресса задач

**Риск:** 👤 Плохой UX — пользователь не видит прогресс

**Файлы:**
- `backend/main.py` — WebSocket endpoint есть (`/ws`)
- `frontend/` — не подключается к WebSocket

Для долгих задач (генерация кода, исследование) пользователь видит только спиннер без информации о прогрессе.

**Статус:** 🟠 Backend готов, frontend не интегрирован

---

### 8. UnifiedChat.tsx — 2278 строк кода

**Риск:** 🔧 Сложность поддержки, риск регрессий

**Файл:** `frontend/src/components/UnifiedChat.tsx`

Компонент нарушает Single Responsibility Principle:
- Рендеринг сообщений
- Markdown парсинг
- Выполнение кода
- Управление состоянием
- Работа с моделями

**Решение:** Разбить на:
- `ChatMessage.tsx`
- `CodeExecutor.tsx`
- `MarkdownRenderer.tsx`
- `ModelSelector.tsx`
- `ConversationSidebar.tsx`

**Статус:** 🟠 Не реализовано

---

### 9. Chat и IDE работают изолированно

**Риск:** 🔗 Chat не знает о проекте, открытом в IDE

**Проблема:** Вкладки Chat и IDE не делят `projectPath`. Chat не может использовать контекст проекта.

**Решение:** Создать общий `ProjectStore` (Zustand):
```typescript
// frontend/src/state/projectStore.ts
export const useProjectStore = create((set) => ({
  projectPath: null,
  projectTree: [],
  setProject: (path, tree) => set({ projectPath: path, projectTree: tree }),
}));
```

**Статус:** 🟠 Не реализовано

---

### 10. LongTermMemory не полностью интегрирована

**Риск:** 🔄 Повторение одних и тех же ошибок

**Файл:** `backend/memory/long_term.py`

Память сохраняет решения и feedback, но НЕ используется для:
- Персонализации ответов
- Избежания повторных ошибок
- Выбора оптимальной модели на основе истории

**Статус:** 🟠 Частично реализовано

---

## 🟡 ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ (низкий приоритет)

### 11. Утечка blob URL в UnifiedChat

**Файл:** `frontend/src/components/UnifiedChat.tsx:710-719`

Cleanup blob URL вызывается только при unmount. При множественных выполнениях кода URL накапливаются.

**Статус:** 🟡 Minor memory leak

---

### 12. Смешанная локализация (RU/EN)

Интерфейс содержит смесь языков: `'Ассистент'`, `'Задачи'`, `'Agents'`.

**Статус:** 🟡 Косметическая проблема

---

### 13. Промпты захардкожены в Python

Промпты встроены в код, а не в конфигурацию — сложно A/B тестировать.

**Решение:** Вынести в `config/prompts.yaml`

**Статус:** 🟡 Технический долг

---

### 14. Нет offline режима

При потере сети — только общее сообщение об ошибке. Нет кэширования, retry, индикатора offline.

**Статус:** 🟡 Nice to have

---

## 📋 Чеклист перед production

- [ ] Добавить rate limiting для LLM API
- [ ] Включить few-shot примеры в промпты агентов
- [ ] Добавить TTL в кэширование
- [ ] Реализовать суммирование длинных чатов
- [ ] Добавить AST валидацию кода
- [ ] Внедрить correlation ID в логи
- [ ] Интегрировать WebSocket в frontend
- [ ] Рефакторинг UnifiedChat.tsx
- [ ] Создать общий ProjectStore для Chat/IDE
- [ ] Настроить Prometheus метрики
- [ ] Написать интеграционные тесты

---

## 🔒 Рекомендации по безопасности

### При деплое на сервер:

1. **Никогда не храните API ключи в config.yaml**
   ```bash
   # Используйте переменные окружения:
   export OPENAI_API_KEY="sk-..."
   export ANTHROPIC_API_KEY="sk-ant-..."
   ```

2. **Ограничьте доступ к API**
   ```python
   # Добавьте аутентификацию в main.py
   from fastapi.security import HTTPBearer
   security = HTTPBearer()
   ```

3. **Включите CORS только для доверенных origins**
   ```python
   app.add_middleware(
       CORSMiddleware,
       allow_origins=["http://localhost:1420"],  # НЕ "*"
       allow_credentials=True,
   )
   ```

4. **Sandbox для выполнения кода**
   ```yaml
   # config.yaml
   tools:
     safety:
       sandbox_enabled: true
       allow_shell: false  # В production
   ```

---

## 📊 Метрики здоровья системы

### Критические пороги:

| Метрика | Норма | Предупреждение | Критично |
|---------|-------|----------------|----------|
| RAM usage | < 2 GB | 2-4 GB | > 4 GB |
| LLM latency (p95) | < 5s | 5-15s | > 15s |
| Error rate | < 1% | 1-5% | > 5% |
| Cache hit rate | > 30% | 10-30% | < 10% |
| Queue depth | < 10 | 10-50 | > 50 |

---

## 🆘 Контакты для эскалации

При обнаружении критических проблем:
1. Создайте issue в репозитории
2. Укажите версию, шаги воспроизведения, логи
3. Для security issues — создайте private issue

---

*Этот документ обновляется при обнаружении новых проблем.*

