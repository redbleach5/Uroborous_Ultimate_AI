# ⚠️ Критические предупреждения и известные проблемы

> **Последнее обновление:** 29 декабря 2025  
> **Версия проекта:** Uroborous Ultimate AI v1.x

---

## 📚 Связанная документация

- **[REFACTORING.md](./REFACTORING.md)** — План рефакторинга сложных функций (69 шт, приоритезированы)
- **[FUTURE_PLAN.md](./FUTURE_PLAN.md)** — Roadmap развития проекта

---

## 🔧 Аудит кода (29 декабря 2025)

Проведён глубокий аудит проекта:
- ✅ **110/110 модулей** импортируются корректно
- ✅ **141 проблема исправлена** (неиспользуемые импорты/переменные, undefined names)
- ✅ **Критические баги устранены** (asyncio в preview.py, циклические импорты)
- ⚠️ **69 функций** требуют рефакторинга (см. REFACTORING.md)

---

## 🚀 НОВЫЕ ВОЗМОЖНОСТИ (29 декабря 2025)

### AST-based Code Intelligence
**Файл:** `backend/project/code_intelligence.py`

Глубокий анализ структуры кода:
- Извлечение функций, классов, методов с docstrings
- Построение графа зависимостей
- Вычисление цикломатической сложности
- Поддержка Python и JavaScript/TypeScript

```python
from backend.project.code_intelligence import get_code_intelligence

ci = get_code_intelligence()
analysis = await ci.analyze_project("/path/to/project")
# {'entities': [...], 'dependency_graph': {...}, 'total_complexity': 150}
```

### Semantic Code Search
**Файл:** `backend/rag/semantic_code_search.py`

Умный поиск по коду на естественном языке:
```python
from backend.rag.semantic_code_search import get_semantic_code_search

search = get_semantic_code_search(vector_store, llm_manager)
await search.index_project("/path/to/project")
results = await search.search("функция для обработки ошибок авторизации")
```

### Incremental Indexer
**Файл:** `backend/project/incremental_indexer.py`

Индексирование только изменённых файлов:
```python
from backend.project.incremental_indexer import get_incremental_indexer

indexer = get_incremental_indexer(vector_store)
stats = await indexer.update_index("/path/to/project")
# {'new_files': 5, 'modified_files': 2, 'unchanged_files': 150}
```

### Enhanced Code Validator (ruff integration)
**Файл:** `backend/core/code_validator.py`

Расширенная валидация с ruff и автоисправлением:
```python
from backend.core.code_validator import get_code_validator

validator = get_code_validator(llm_manager)
result = await validator.validate(code, language="python", fix_errors=True)
# result.is_valid, result.issues, result.fixed_code
```

### Self-Consistency Mixin
**Файл:** `backend/agents/self_consistency_mixin.py`

Генерация нескольких вариантов с выбором консенсуса:
```python
result = await agent.generate_with_self_consistency(
    messages=messages,
    n_samples=3,
    extract_answer_fn=extract_code_answer
)
# result.final_answer, result.confidence, result.agreement_score
```

### Multi-Agent Synthesis
**Файл:** `backend/core/multi_agent_synthesis.py`

Синтез результатов от нескольких агентов:
```python
from backend.core.multi_agent_synthesis import get_multi_agent_synthesizer, SynthesisStrategy

synthesizer = get_multi_agent_synthesizer(llm_manager)
result = await synthesizer.synthesize(
    results=agent_results,
    original_task="Analyze the project",
    strategy=SynthesisStrategy.MERGE
)
```

### Code Intelligence API
**Эндпоинты:**
- `POST /api/v1/code-intelligence/analyze/project` — Анализ проекта
- `POST /api/v1/code-intelligence/analyze/file` — Анализ файла
- `POST /api/v1/code-intelligence/index` — Индексирование
- `POST /api/v1/code-intelligence/validate` — Валидация кода
- `POST /api/v1/code-intelligence/find-entity` — Поиск сущности

---

## ✅ ИСПРАВЛЕНО (недавние улучшения)

### ~~1. Rate Limiting для LLM провайдеров~~ ✅ ИСПРАВЛЕНО

**Реализовано в:** `backend/core/rate_limiter.py`, `backend/main.py`

Rate limiting теперь полностью работает:
- Sliding window алгоритм
- Настраиваемые лимиты: requests_per_minute, requests_per_hour, requests_per_day
- Интегрирован как middleware в FastAPI

---

### ~~2. Few-shot примеры в промптах~~ ✅ ИСПРАВЛЕНО

**Реализовано в:** `backend/agents/base.py`

Агенты теперь автоматически загружают успешные примеры из LongTermMemory:
- `_get_few_shot_examples()` - поиск качественных примеров
- `_enhance_prompt_with_examples()` - интеграция в промпты
- Учитывается quality_score для выбора лучших примеров

---

### ~~3. TTL в кэшировании~~ ✅ ИСПРАВЛЕНО

**Реализовано в:** `backend/core/advanced_cache.py`, `backend/rag/cache.py`

Кэширование теперь безопасно:
- LRU eviction с ограничением `memory_size`
- TTL (time-to-live) для автоматического устаревания
- Disk cache с периодической очисткой

---

### ~~4. История чата с суммированием~~ ✅ ИСПРАВЛЕНО

**Реализовано в:** `backend/api/routers/chat.py`

Длинные диалоги теперь обрабатываются умно:
- `summarize_old_messages()` - суммирование через LLM
- `prepare_chat_history()` - интеллектуальная подготовка истории
- Сохраняется контекст при >20 сообщениях

---

### ~~5. Валидация синтаксиса кода~~ ✅ ИСПРАВЛЕНО

**Реализовано в:** `backend/agents/code_writer.py`

CodeWriterAgent теперь валидирует сгенерированный код:
- `validate_python_syntax()` - проверка через AST
- `validate_javascript_syntax()` - базовая валидация скобок
- Автоматические попытки исправления через LLM (до 2 retry)

---

### ~~6. Correlation ID для логов~~ ✅ ИСПРАВЛЕНО

**Реализовано в:** `backend/core/logger.py`, `backend/main.py`

Логирование теперь с трассировкой:
- `correlation_id` через ContextVar
- Logging middleware добавляет ID к каждому запросу
- Можно отследить запрос через все компоненты

---

### ~~7. WebSocket для прогресса задач~~ ✅ ИСПРАВЛЕНО

**Реализовано в:** `frontend/src/hooks/useWebSocket.ts`, `frontend/src/components/UnifiedChat.tsx`

WebSocket интеграция:
- Включен по умолчанию для лучшего UX
- Real-time обновления прогресса
- Автоматическое переподключение

---

### ~~8. UnifiedChat.tsx рефакторинг~~ ✅ ЧАСТИЧНО ИСПРАВЛЕНО

**Реализовано в:** `frontend/src/components/`

Компонент разделен (938 строк вместо 2278):
- `ChatMessage.tsx` - рендеринг сообщений
- `CodeExecutor.tsx` - выполнение кода
- `ModelSelector.tsx` - выбор моделей
- `ConversationSidebar.tsx` - боковая панель

---

### ~~9. ProjectStore для Chat/IDE~~ ✅ ИСПРАВЛЕНО

**Реализовано в:** `frontend/src/state/projectStore.ts`

Создан общий store для связи Chat и IDE:
- Хранение текущего проекта и дерева файлов
- Открытые файлы и активный файл
- `getContextForChat()` - контекст проекта для чата

---

### ~~11. Утечка blob URL~~ ✅ ИСПРАВЛЕНО

**Реализовано в:** `frontend/src/components/CodeExecutor.tsx`

Blob URL теперь очищаются корректно:
- Tracked blob URLs через useRef
- Cleanup при создании новых URL
- Cleanup при unmount

---

### ~~Retry для Ollama~~ ✅ ИСПРАВЛЕНО

**Реализовано в:** `backend/llm/ollama_provider.py`

Добавлен exponential backoff при ошибках:
- До 3 retry attempts
- Экспоненциальная задержка (1s, 2s, 4s)
- Jitter для предотвращения thundering herd

---

### ~~10. LongTermMemory полностью интегрирована~~ ✅ ИСПРАВЛЕНО

**Реализовано в:** `backend/memory/long_term.py`, `backend/agents/base.py`, `backend/api/routers/learning.py`

Полная интеграция долгосрочной памяти:
- **Персонализация:** Таблица `user_preferences` для хранения предпочтений пользователя (язык, стиль кода, детализация)
- **Избежание ошибок:** Таблица `failed_tasks` с семантическим поиском похожих ошибок, предупреждения в промптах
- **Рекомендации моделей:** Таблица `model_task_performance` для выбора оптимальной модели по типу задачи
- **API эндпоинты:** `/learning/preferences`, `/learning/memory/stats`, `/learning/memory/models`, `/learning/memory/failed`

---

## 🟠 НЕТ КРИТИЧЕСКИХ ПРОБЛЕМ

---

## 🟡 ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ (низкий приоритет)

### 12. Смешанная локализация (RU/EN)

Интерфейс содержит смесь языков: `'Ассистент'`, `'Задачи'`, `'Agents'`.

**Статус:** 🟡 Косметическая проблема

---

### 13. Промпты захардкожены в Python

Промпты встроены в код, а не в конфигурацию — сложно A/B тестировать.

**Решение:** Вынести в `config/prompts.yaml`

**Статус:** 🟡 Технический долг (запланировано)

---

### 14. Нет offline режима

При потере сети — только общее сообщение об ошибке. Нет кэширования, retry, индикатора offline.

**Статус:** 🟡 Nice to have

---

## 📋 Чеклист перед production

- [x] Добавить rate limiting для LLM API ✅
- [x] Включить few-shot примеры в промпты агентов ✅
- [x] Добавить TTL в кэширование ✅
- [x] Реализовать суммирование длинных чатов ✅
- [x] Добавить AST валидацию кода ✅
- [x] Внедрить correlation ID в логи ✅
- [x] Интегрировать WebSocket в frontend ✅
- [x] Рефакторинг UnifiedChat.tsx ✅
- [x] Создать общий ProjectStore для Chat/IDE ✅
- [x] Полная интеграция LongTermMemory ✅
- [ ] Настроить Prometheus метрики
- [ ] Написать интеграционные тесты

---

## 🔒 Рекомендации по безопасности

### При деплое на сервер:

1. **Никогда не храните API ключи в config.yaml**
   ```bash
   # Используйте переменные окружения:
   export OPENAI_API_KEY="sk-..."
   export ANTHROPIC_API_KEY="sk-ant-..."
   ```

2. **Ограничьте доступ к API**
   ```python
   # Добавьте аутентификацию в main.py
   from fastapi.security import HTTPBearer
   security = HTTPBearer()
   ```

3. **Включите CORS только для доверенных origins**
   ```python
   app.add_middleware(
       CORSMiddleware,
       allow_origins=["http://localhost:1420"],  # НЕ "*"
       allow_credentials=True,
   )
   ```

4. **Sandbox для выполнения кода**
   ```yaml
   # config.yaml
   tools:
     safety:
       sandbox_enabled: true
       allow_shell: false  # В production
   ```

---

## 📊 Метрики здоровья системы

### Критические пороги:

| Метрика | Норма | Предупреждение | Критично |
|---------|-------|----------------|----------|
| RAM usage | < 2 GB | 2-4 GB | > 4 GB |
| LLM latency (p95) | < 5s | 5-15s | > 15s |
| Error rate | < 1% | 1-5% | > 5% |
| Cache hit rate | > 30% | 10-30% | < 10% |
| Queue depth | < 10 | 10-50 | > 50 |

---

## 🆘 Контакты для эскалации

При обнаружении критических проблем:
1. Создайте issue в репозитории
2. Укажите версию, шаги воспроизведения, логи
3. Для security issues — создайте private issue

---

*Этот документ обновляется при обнаружении новых проблем.*

