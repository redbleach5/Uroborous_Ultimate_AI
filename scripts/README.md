# Скрипты проекта AILLM

Эта директория содержит все скрипты для управления проектом.

## Скрипты

### start_project.sh
Запуск backend и frontend серверов (основной скрипт).

**Использование:**
```bash
# Из корня проекта (рекомендуется)
./start.sh

# Или напрямую
./scripts/start_project.sh
```

**Переменные окружения:**
- `BACKEND_HOST` - хост для backend (по умолчанию: localhost)
- `BACKEND_PORT` - порт для backend (по умолчанию: 8000)
- `FRONTEND_HOST` - хост для frontend (по умолчанию: localhost)
- `FRONTEND_PORT` - порт для frontend (по умолчанию: 1420)
- `VENV_PATH` - путь к виртуальному окружению (по умолчанию: .venv)

**Возможности:**
- Автоматический подбор свободных портов
- Проверка зависимостей
- Запуск с логированием
- Сохранение PID файлов для управления процессами

---

### stop_project.sh
Остановка backend и frontend серверов (основной скрипт).

**Использование:**
```bash
# Из корня проекта (рекомендуется)
./stop.sh

# Или напрямую
./scripts/stop_project.sh
```

**Возможности:**
- Graceful shutdown процессов
- Принудительная остановка при необходимости
- Очистка PID файлов
- Проверка и завершение зависших процессов

---

### cleanup_project.py
Очистка проекта от временных файлов, кэша и мусорных файлов.

**Использование:**
```bash
./scripts/cleanup_project.py
# или
python3 scripts/cleanup_project.py
```

**Что очищается:**
- Python кэш (`__pycache__`, `.pyc` файлы)
- Временные файлы Python (`.egg-info`, кэши тестов)
- Файлы установки Python (если есть)
- Тестовые файлы
- OS файлы (`.DS_Store`, `Thumbs.db`, swap файлы)
- Логи проекта (`logs/`, `LOGS_DEBUG/`, `frontend/logs/`)
- PID файлы (только для остановленных процессов)
- Кэш node_modules
- Обновление `.gitignore` при необходимости

**Безопасность:**
- Резервные копии конфигурации не удаляются автоматически
- PID файлы запущенных процессов не удаляются
- Важные файлы (README.md, monitor_state.json) сохраняются

---

### check_syntax.py
Проверка синтаксиса всех Python файлов в проекте.

**Использование:**
```bash
./scripts/check_syntax.py
# или
python3 scripts/check_syntax.py
```

**Что проверяется:**
- Все файлы в директориях `backend/`, `tests/`, `examples/`
- Файлы `run.py`, `setup.py` (если есть)

**Результат:**
- ✅ Все файлы корректны
- ❌ Список файлов с ошибками синтаксиса

---

### update_logging_imports.py
Автоматическое обновление импортов логирования во всех файлах проекта.

**Использование:**
```bash
./scripts/update_logging_imports.py
# или
python3 scripts/update_logging_imports.py
```

**Что делает:**
- Заменяет прямые импорты `loguru` на использование централизованного `get_logger`
- Правильно определяет относительные пути импорта
- Обновляет только файлы, которые используют старый способ логирования

**Примечание:**
Этот скрипт уже был выполнен для всех файлов проекта. Повторный запуск не изменит уже обновленные файлы.

---

## Структура путей

Все скрипты автоматически определяют корень проекта (директорию на уровень выше `scripts/`) и работают относительно него. Это позволяет запускать скрипты из любой директории проекта.

**Формула определения корня:**
```python
# Python
PROJECT_ROOT = Path(__file__).parent.parent

# Bash
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
```

---

## Рекомендации

1. **Запуск проекта:** Используйте `start_project.sh` для запуска
2. **Остановка проекта:** Используйте `stop_project.sh` для корректной остановки
3. **Очистка проекта:** Запускайте `cleanup_project.py` периодически для освобождения места
4. **Проверка синтаксиса:** Используйте `check_syntax.py` перед коммитом

---

### validate_project.py
Комплексная валидация проекта с поддержкой автоматического исправления.

**Использование:**
```bash
# Базовая проверка
python3 scripts/validate_project.py

# Автоматическое исправление проблем
python3 scripts/validate_project.py --auto-fix

# Посмотреть что будет исправлено (без изменений)
python3 scripts/validate_project.py --auto-fix --dry-run
```

**Что проверяет:**
- ✅ Структура директорий проекта (AUTO-FIX)
- ✅ Синтаксис всех Python файлов
- ✅ Корректность импортов и путей (AUTO-FIX)
- ✅ Использование централизованного логирования (AUTO-FIX)
- ✅ Конфигурационные файлы
- ✅ Согласованность скриптов (AUTO-FIX)
- ✅ Наличие необходимой документации
- 🧠 Анализ зависимостей между модулями

**Автоматически исправляет:**
- Неправильные импорты логирования
- Неправильные пути в скриптах
- Неорганизованную структуру файлов
- Отсутствующие директории

**Результат:**
- ❌ Ошибки (блокируют коммит)
- ⚠️ Предупреждения (информационные)
- 🔧 [AUTO-FIX] - можно исправить автоматически
- ✅ Успешная валидация

**Подробнее:** См. [docs/VALIDATION.md](../docs/VALIDATION.md)

---

### setup_git_hooks.sh
Установка git hooks для автоматической валидации при коммите.

**Использование:**
```bash
./scripts/setup_git_hooks.sh
```

**Что делает:**
- Устанавливает pre-commit hook
- Автоматически запускает валидацию проекта перед каждым коммитом
- Проверяет синтаксис измененных Python файлов

**Преимущества:**
- Автоматическое обнаружение проблем до коммита
- Единообразие кода и структуры
- Предотвращение простых ошибок

**Пропуск проверок (не рекомендуется):**
```bash
git commit --no-verify
```

---

## Устранение неполадок

### Скрипты не запускаются
Убедитесь, что у скриптов есть права на выполнение:
```bash
chmod +x scripts/*.sh scripts/*.py
```

### Неправильные пути
Все скрипты автоматически определяют корень проекта. Если возникают проблемы с путями, убедитесь, что структура директорий корректна:
```
project_root/
├── scripts/
│   ├── start_project.sh
│   ├── stop_project.sh
│   ├── cleanup_project.py
│   └── ...
├── backend/
├── frontend/
└── ...
```

### PID файлы не находятся
PID файлы создаются в корне проекта. Скрипты автоматически определяют правильный путь, но если процессы запускались не через `start_project.sh`, PID файлы могут находиться в другом месте.

