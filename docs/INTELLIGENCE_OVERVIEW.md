# 🧠 Интеллектуальные возможности AILLM

Этот документ описывает "умные" компоненты системы, которые делают её самообучающейся и адаптивной.

---

## Оценка системы

| Компонент | Оценка | Описание |
|-----------|--------|----------|
| **ReflectionMixin** | ⭐⭐⭐⭐⭐ | Превосходная система самокоррекции |
| **LearningSystem** | ⭐⭐⭐⭐⭐ | Полноценное персистентное обучение |
| **IntelligentModelRouter** | ⭐⭐⭐⭐ | Умный динамический скоринг |
| **RAG система** | ⭐⭐⭐⭐ | Мощная, с суммированием |
| **Inter-Agent Communication** | ⭐⭐⭐⭐ | Делегирование и broadcast |
| **Performance Tracking** | ⭐⭐⭐⭐⭐ | Детальные метрики с историей |

**Общий уровень интеллекта: 8.5/10**

---

## 1. Система обучения (LearningSystem)

### Описание

`LearningSystem` — это персистентная система, которая накапливает опыт выполнения задач и использует его для улучшения будущих результатов.

### Расположение

`backend/core/learning_system.py`

### Возможности

#### 1.1 Запись рефлексий

```python
await learning_system.record_reflection(
    agent_name="code_writer",
    task="Напиши функцию сортировки",
    reflection={
        "completeness": 95,
        "correctness": 100,
        "quality": 90,
        "overall_score": 95
    },
    was_corrected=False,
    correction_attempts=1,
    execution_time=2.5,
    solution_snippet="def quicksort(arr): ..."
)
```

#### 1.2 Улучшение промптов на основе опыта

```python
enhancement = await learning_system.get_prompt_enhancement("code_writer", task)
# Returns:
# --- ОБУЧЕНИЕ НА ОПЫТЕ ---
# ⚠️ ВАЖНО (из опыта 150 задач): Избегайте проблем: неполная валидация входных данных
# 📊 Историческая корректность: 85% — уделите особое внимание правильности.
# 💡 Пример успешного решения похожей задачи (качество 95%):
# def validate_input(data): ...
# ---
```

#### 1.3 Few-Shot Learning

Система автоматически сохраняет успешные решения (quality_score >= 85) как примеры для будущих задач.

#### 1.4 Статистика по агентам

```python
insights = await learning_system.get_agent_insights("code_writer")
# {
#   "status": "ok",
#   "stats": {
#     "total_tasks": 150,
#     "successful_tasks": 127,
#     "success_rate": 0.847,
#     "avg_quality_score": 82.5
#   },
#   "common_issues": [
#     {"issue": "Неполная валидация", "count": 15},
#     {"issue": "Отсутствие docstring", "count": 8}
#   ],
#   "recommendations": [...]
# }
```

---

## 2. Система рефлексии (ReflectionMixin)

### Описание

Миксин, добавляющий агентам способность анализировать свои результаты и автоматически исправлять ошибки.

### Расположение

`backend/agents/reflection_mixin.py`

### Цикл рефлексии

```
┌─────────────┐
│   Execute   │
│    Task     │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Reflect   │◀──────────────┐
│   on Result │               │
└──────┬──────┘               │
       │                      │
       ▼                      │
┌─────────────┐     NO   ┌────┴────┐
│  Quality OK │─────────▶│  Retry  │
│    (≥60)?   │          │  (max 2)│
└──────┬──────┘          └─────────┘
       │ YES
       ▼
┌─────────────┐
│   Return    │
│   Result    │
└─────────────┘
```

### Критерии оценки

| Критерий | Вес | Описание |
|----------|-----|----------|
| Completeness | 35% | Полнота решения |
| Correctness | 45% | Корректность |
| Quality | 20% | Качество кода/текста |

### Уровни качества

| Уровень | Диапазон |
|---------|----------|
| EXCELLENT | 90-100 |
| GOOD | 70-89 |
| ACCEPTABLE | 50-69 |
| POOR | 30-49 |
| FAILED | 0-29 |

### Использование

```python
class MyAgent(BaseAgent, ReflectionMixin):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self.configure_reflection(
            enabled=True,
            max_retries=2,
            min_quality_threshold=60.0
        )
```

---

## 3. Интеллектуальный роутинг моделей

### Описание

Система автоматически выбирает оптимальную модель для каждой задачи на основе множества факторов.

### Расположение

`backend/core/intelligent_model_router.py`

### Компоненты скоринга

#### 3.1 Профиль модели (ModelProfile)

Автоматически определяется по имени модели:

```python
profile = ModelProfile.from_model_name("deepseek-coder:6.7b")
# ModelProfile(
#   name="deepseek-coder:6.7b",
#   size_b=6.7,
#   capabilities={
#     CODE_GENERATION: 0.9,
#     INSTRUCTION_FOLLOWING: 0.8
#   },
#   base_speed_score=0.78,
#   quality_score=0.635
# )
```

#### 3.2 Требования задачи (TaskRequirements)

```python
requirements = TaskRequirements.from_task_analysis(
    task="Напиши функцию для парсинга JSON",
    task_type="code",
    complexity="medium"
)
# TaskRequirements(
#   required_capabilities={CODE_GENERATION: 0.8},
#   min_quality=0.7,
#   prefer_speed=False,
#   estimated_tokens=500
# )
```

#### 3.3 Формула скоринга

```
total_score = (
    capability_score * 0.30 +
    performance_score * 0.30 +
    speed_score * 0.20 +
    quality_score * 0.20
)
```

Для быстрых задач (prefer_speed=True):
```
total_score = (
    capability_score * 0.10 +
    performance_score * 0.20 +
    speed_score * 0.50 +
    quality_score * 0.20
)
```

### Возможности моделей (Capabilities)

| Capability | Описание |
|------------|----------|
| FAST_RESPONSE | Быстрый ответ |
| CODE_GENERATION | Генерация кода |
| REASONING | Логические рассуждения |
| MULTILINGUAL | Многоязычность |
| INSTRUCTION_FOLLOWING | Следование инструкциям |
| CREATIVE | Креативность |
| FACTUAL | Фактологическая точность |
| LONG_CONTEXT | Длинный контекст |

---

## 4. Трекинг производительности (Performance Tracker)

### Описание

Система отслеживает производительность каждой модели и использует эти данные для оптимизации выбора.

### Расположение

`backend/core/model_performance_tracker.py`

### Отслеживаемые метрики

```python
@dataclass
class ModelMetrics:
    model_name: str
    provider: str
    
    # Статистика запросов
    total_requests: int
    successful_requests: int
    failed_requests: int
    
    # Время выполнения
    avg_duration: float
    min_duration: float
    max_duration: float
    
    # Токены
    avg_tokens_per_sec: float
    
    # Рейтинг
    performance_score: float  # 0-100
```

### Расчёт performance_score

```
score = 0

# Success rate (0-50 баллов)
score += (successful / total) * 50

# Скорость (0-30 баллов)
score += min(avg_tokens_per_sec / 100 * 30, 30)

# Стабильность (0-20 баллов)
cv = std_dev / avg_duration  # Коэффициент вариации
score += max(0, 20 - cv * 20)
```

### Learning Insights

```python
insights = await tracker.get_learning_insights()
# {
#   "status": "ok",
#   "total_experience": 5000,
#   "models_analyzed": 12,
#   "top_performers": [
#     {"model": "qwen2.5-coder:14b", "score": 92.5, "success_rate": 0.98},
#     {"model": "deepseek-coder:6.7b", "score": 87.3, "success_rate": 0.95}
#   ],
#   "underperformers": [...],
#   "recommendations": [
#     "Предпочитайте модель qwen2.5-coder:14b - лучший performance score 92.5"
#   ]
# }
```

---

## 5. Долгосрочная память с качеством

### Описание

Память хранит решения и использует quality scoring для приоритизации лучших.

### Расположение

`backend/memory/long_term.py`

### Quality Score

```
quality_score = (
    (avg_rating / 5) * 40 +      # 0-40 баллов от рейтинга
    helpful_rate * 40 +           # 0-40 баллов от полезности
    min(feedback_count / 10, 1) * 20  # 0-20 баллов от количества
)
```

### Поиск с учётом качества

```python
results = await memory.search_similar_tasks_with_quality(
    task="Напиши функцию парсинга",
    top_k=5,
    min_quality=20.0
)
# Результаты отсортированы по combined_score:
# combined_score = similarity * 0.6 + (quality_score / 100) * 0.4
```

---

## 6. Inter-Agent Communication

### Описание

Агенты могут делегировать подзадачи друг другу и обмениваться сообщениями.

### Расположение

`backend/agents/communicator.py`, `backend/agents/base.py`

### Делегирование задач

```python
class ResearchAgent(BaseAgent):
    async def _execute_impl(self, task, context):
        # Делегируем генерацию кода другому агенту
        code_result = await self.delegate_to(
            agent_type="code_writer",
            subtask="Напиши функцию для парсинга данных",
            context={"format": "json"}
        )
        
        # Используем результат в своей работе
        return self.analyze(code_result)
```

### Запрос помощи по capability

```python
result = await self.request_help(
    capability="code_generation",
    task="Создать утилиту для работы с API",
    context=context
)
```

### Broadcast

```python
await self.broadcast_message({
    "type": "status_update",
    "progress": 50,
    "message": "Половина работы выполнена"
})
```

---

## 7. Thinking Mode

### Описание

Режим "thinking" позволяет моделям выполнять более глубокий анализ перед ответом.

### Автоматическое включение

Thinking mode автоматически включается для:
- Декомпозиции задач в Orchestrator
- Выбора агента (agent selection)
- Сложных задач в ReactAgent

```python
response = await self.llm_manager.generate(
    messages=messages,
    thinking_mode=True  # Включаем глубокое размышление
)

if response.has_thinking:
    logger.debug(f"Thinking trace: {response.thinking}")
```

---

## 8. Uncertainty Search (UncertaintySearchMixin)

### Описание

Автоматический поиск информации, когда агент не уверен в ответе.

### Расположение

`backend/agents/uncertainty_search_mixin.py`

### Триггеры поиска

- Вопросы о текущих событиях/датах
- Неуверенность в фактах
- Запросы актуальной информации

---

## Рекомендации по использованию

### Когда использовать Thinking Mode

✅ Используйте для:
- Планирования сложных задач
- Анализа архитектуры
- Принятия важных решений
- Отладки сложных проблем

❌ Не используйте для:
- Простых вопросов
- Форматирования кода
- Генерации boilerplate

### Когда полагаться на Learning System

После 50+ задач система начинает давать качественные рекомендации:
- Промпты автоматически улучшаются
- Избегаются повторяющиеся ошибки
- Используются проверенные паттерны решений

### Оптимизация Performance Tracker

Для получения качественных рекомендаций:
- Минимум 5 запросов на модель
- Разнообразные типы задач
- Достаточный временной период (1+ неделя)

---

## Метрики успешности

Система предоставляет следующие метрики для оценки интеллекта:

| Метрика | API Endpoint | Описание |
|---------|--------------|----------|
| Learning Stats | `GET /api/v1/feedback/stats` | Статистика обучения |
| Model Performance | `GET /api/v1/monitoring/models` | Производительность моделей |
| Agent Insights | `GET /api/v1/agents/{name}/insights` | Инсайты по агенту |

---

## Дальнейшее развитие

См. [FUTURE_PLAN.md](../FUTURE_PLAN.md) для планов по улучшению:
- Multi-Agent Collaboration
- Confidence Estimation
- Chain-of-Thought Verification
- Tool Use Learning

