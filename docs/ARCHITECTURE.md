# Архитектура AILLM

## Обзор

AILLM (Uroborous Ultimate AI) — это комплексная система автономных агентов для разработки ПО на основе LLM. Система построена на принципах модульности, расширяемости и безопасности.

**Ключевые особенности:**
- 🧠 **Самообучение** — система учится на опыте выполнения задач
- 🔄 **Рефлексия** — агенты анализируют и улучшают свои результаты
- 🎯 **Интеллектуальный роутинг** — автоматический выбор оптимальной модели
- 💾 **Персистентность** — сохранение опыта между сессиями

---

## Интеллектуальные подсистемы

### Learning System (Система обучения)

**Расположение**: `backend/core/learning_system.py`

Персистентная система обучения на опыте:

```
┌─────────────────────────────────────────────────────────────────┐
│                     LearningSystem                              │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ Reflection      │  │ Successful      │  │ Error           │ │
│  │ History         │  │ Solutions       │  │ Patterns        │ │
│  │ (статистика)    │  │ (few-shot)      │  │ (предупреждения)│ │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘ │
│           │                    │                    │          │
│           └────────────────────┼────────────────────┘          │
│                                ▼                               │
│                    ┌─────────────────────┐                     │
│                    │  Prompt Enhancement │                     │
│                    │  (улучшение задач)  │                     │
│                    └─────────────────────┘                     │
└─────────────────────────────────────────────────────────────────┘
```

**Возможности:**
- Запись результатов рефлексии для анализа
- Сохранение успешных решений как примеров (few-shot learning)
- Отслеживание паттернов ошибок и их решений
- Автоматическое улучшение промптов на основе накопленного опыта
- Персистентность в SQLite между сессиями

### Reflection System (Система рефлексии)

**Расположение**: `backend/agents/reflection_mixin.py`

Самокоррекция агентов:

```
┌───────────────────────────────────────────────────────────┐
│                    Reflection Loop                        │
├───────────────────────────────────────────────────────────┤
│                                                           │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌────────┐ │
│  │ Execute │───▶│ Reflect │───▶│ Quality │───▶│ Retry? │ │
│  │  Task   │    │ Result  │    │  Check  │    │        │ │
│  └─────────┘    └─────────┘    └─────────┘    └───┬────┘ │
│       ▲                                           │      │
│       │              ┌─────────────────┐          │      │
│       └──────────────│  Self-Correct   │◀─────────┘      │
│                      │  (до 2 раз)     │                 │
│                      └─────────────────┘                 │
└───────────────────────────────────────────────────────────┘
```

**Критерии оценки:**
- **Completeness** (0-100) — полнота решения
- **Correctness** (0-100) — корректность решения
- **Quality** (0-100) — качество кода/текста
- **Overall Score** = 35% completeness + 45% correctness + 20% quality

### Intelligent Model Router (Интеллектуальный роутинг)

**Расположение**: `backend/core/intelligent_model_router.py`

Динамический выбор модели:

```
┌─────────────────────────────────────────────────────────────────┐
│                  IntelligentModelRouter                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Task Analysis          Model Capabilities      Performance     │
│  ┌─────────────┐        ┌─────────────────┐    ┌────────────┐  │
│  │ • type      │        │ • code_gen: 0.9 │    │ success: 95%│  │
│  │ • complexity│───────▶│ • reasoning: 0.8│───▶│ speed: 50t/s│  │
│  │ • tokens    │        │ • multilang: 0.7│    │ score: 85   │  │
│  └─────────────┘        └─────────────────┘    └────────────┘  │
│                                                       │         │
│                                                       ▼         │
│                                              ┌──────────────┐   │
│                                              │ Best Model   │   │
│                                              │ + Server     │   │
│                                              └──────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

**Факторы скоринга:**
- Capability Score (30%) — соответствие возможностей требованиям
- Performance Score (30%) — историческая производительность
- Speed Score (20%) — скорость генерации
- Quality Score (20%) — базовое качество модели

### Model Performance Tracker

**Расположение**: `backend/core/model_performance_tracker.py`

Отслеживание производительности моделей:

**Метрики:**
- Total/successful/failed requests
- Average/min/max duration
- Tokens per second
- Error types distribution
- Performance score (0-100)

**Возможности:**
- Персистентность в SQLite
- Рекомендации лучших моделей
- Learning insights — анализ паттернов

---

## Основные компоненты

### 1. Core Engine (IDAEngine)

**Расположение**: `backend/core/engine.py`

Центральный координатор всех компонентов системы:
- Управление жизненным циклом компонентов
- Инициализация и shutdown всех подсистем
- Координация взаимодействия между компонентами
- Обработка ошибок и восстановление
- Интеграция с Learning System

### 2. LLM Provider Manager

**Расположение**: `backend/llm/`

Поддержка множественных LLM провайдеров:
- **OpenAI Provider** (`openai_provider.py`): Интеграция с OpenAI API
- **Anthropic Provider** (`anthropic_provider.py`): Интеграция с Anthropic Claude API
- **Ollama Provider** (`ollama_provider.py`): Поддержка локальных моделей через Ollama

Особенности:
- Автоматический выбор провайдера
- Fallback механизмы при ошибках
- Кэширование ответов
- Приоритет локальным моделям (Ollama) для экономии
- **Thinking Mode** для сложных задач

### 3. RAG Система

**Расположение**: `backend/rag/`

#### Vector Store (`vector_store.py`)
- Семантический поиск с использованием FAISS
- BM25 для текстового поиска
- Генерация эмбеддингов через sentence-transformers
- Re-ranking результатов

#### Context Manager (`context_manager.py`)
- Иерархическое управление контекстом
- Query expansion для улучшения поиска
- Multi-query retrieval
- История взаимодействий
- **Автоматическое суммирование** длинного контекста

### 4. Агенты

**Расположение**: `backend/agents/`

#### BaseAgent (`base.py`)
Базовый класс со встроенными mixins:
- **ReflectionMixin** — самокоррекция результатов
- **UncertaintySearchMixin** — поиск при неуверенности
- **Inter-Agent Communication** — делегирование задач

#### Специализированные агенты:

| Агент | Назначение |
|-------|------------|
| **CodeWriterAgent** | Генерация, рефакторинг кода, создание тестов |
| **ReactAgent** | ReAct подход: Thought → Action → Observation |
| **ResearchAgent** | Исследование кодовой базы, анализ |
| **DataAnalysisAgent** | Анализ данных, ML, визуализация |
| **WorkflowAgent** | Управление пайплайнами |
| **IntegrationAgent** | Интеграция с внешними сервисами |
| **MonitoringAgent** | Мониторинг и метрики |

### 5. Orchestrator

**Расположение**: `backend/orchestrator.py`

Умный оркестратор задач:
- Декомпозиция сложных задач с помощью LLM
- Интеллектуальный выбор агента (LLMClassifier)
- Последовательное выполнение с накоплением контекста
- Параллельное выполнение независимых задач
- Auto-recovery с fallback агентом

### 6. Система инструментов

**Расположение**: `backend/tools/`

| Инструмент | Файл | Описание |
|------------|------|----------|
| File Tools | `file_tools.py` | Чтение, запись, список файлов |
| Shell Tools | `shell_tools.py` | Выполнение shell команд |
| Git Tools | `git_tools.py` | Git операции |
| Web Search | `web_tools.py` | Поиск в интернете |
| API Call | `api_tools.py` | HTTP запросы к API |
| Database | `db_tools.py` | SQL запросы |

### 7. Safety Guard

**Расположение**: `backend/safety/guard.py`

Защита от опасных операций:
- Валидация команд (защита от `rm -rf`, `format`, etc.)
- Валидация путей (защита от path traversal)
- Валидация URL (защита от SSRF)
- Блокировка опасных паттернов

### 8. Long Term Memory

**Расположение**: `backend/memory/long_term.py`

Память с обучением:
- Сохранение успешных решений
- Семантический поиск похожих задач
- **Quality scoring** на основе feedback
- Приоритет качественным решениям
- Автоматическая очистка низкокачественных записей

### 9. API Server

**Расположение**: `backend/main.py`, `backend/api/routers/`

FastAPI приложение:
- RESTful API endpoints
- WebSocket для real-time коммуникации
- Feedback API для обучения
- Learning Dashboard API

### 10. Frontend

**Расположение**: `frontend/`

React + TypeScript + Tauri:
- **MainLayout**: Главный layout с табами
- **CodeEditor**: Monaco Editor
- **LearningDashboard**: Визуализация обучения
- **UnifiedChat**: Единый интерфейс чата

---

## Поток данных

```
User Request
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│                     API Server / WebSocket                      │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│                         IDAEngine                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Learning     │  │ Performance  │  │ Resource     │          │
│  │ System       │  │ Tracker      │  │ Selector     │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│                       Orchestrator                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Task         │  │ LLM          │  │ Task         │          │
│  │ Decomposition│  │ Classifier   │  │ Router       │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Agent Execution                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Reflection   │  │ Tool         │  │ Context      │          │
│  │ Loop         │  │ Registry     │  │ Manager      │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│         │                                                       │
│         ▼                                                       │
│  ┌──────────────────────────────────────────────────┐          │
│  │              LLM Provider Manager                │          │
│  │  ┌────────┐  ┌────────┐  ┌────────┐             │          │
│  │  │ Ollama │  │ OpenAI │  │Anthropic│            │          │
│  │  └────────┘  └────────┘  └────────┘             │          │
│  └──────────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Long Term Memory                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Save         │  │ Quality      │  │ Semantic     │          │
│  │ Solution     │  │ Scoring      │  │ Search       │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
User Response (with learning recorded)
```

---

## Конфигурация

Все настройки в `config/config.yaml`:
- LLM провайдеры и их параметры
- RAG настройки
- Агенты и их конфигурация
- Безопасность
- Производительность
- Distributed mode (несколько Ollama серверов)

---

## Расширяемость

### Добавление нового агента

```python
from backend.agents.base import BaseAgent

class MyAgent(BaseAgent):
    async def _execute_impl(self, task: str, context: Dict[str, Any]) -> Dict[str, Any]:
        # Логика агента
        result = await self._get_llm_response(messages)
        return {"success": True, "result": result}
```

### Добавление нового инструмента

```python
from backend.tools.base import BaseTool

class MyTool(BaseTool):
    async def execute(self, **params) -> ToolResult:
        # Логика инструмента
        return ToolResult(success=True, result=data)
```

### Добавление нового LLM провайдера

```python
from backend.llm.base import BaseLLMProvider

class MyProvider(BaseLLMProvider):
    async def generate(self, messages: List[LLMMessage], **kwargs) -> LLMResponse:
        # Логика провайдера
        return LLMResponse(content=response)
```

---

## Безопасность

- Safety Guard проверяет все команды и пути
- Изоляция выполнения (sandbox mode)
- Валидация всех входных данных
- Защита от SSRF/XSS
- Блокировка опасных паттернов

---

## Производительность

- Кэширование LLM ответов
- Кэширование эмбеддингов
- Кэширование контекста
- Параллельное выполнение задач
- Ленивая загрузка компонентов
- Оптимизация для локального использования (Ollama)
- Персистентные метрики для оптимизации выбора моделей

---

## Будущие улучшения

Подробный план в [FUTURE_PLAN.md](../FUTURE_PLAN.md):
- Консолидация систем роутинга моделей
- Streaming для всех агентов
- Проверка на галлюцинации (FactChecker)
- Multi-Agent Collaboration
- Визуальный конструктор workflow
