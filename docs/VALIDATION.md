# Валидация проекта

Система автоматической валидации проекта AILLM для поддержания порядка, согласованности и обнаружения проблем на раннем этапе.

## 📋 Обзор

Валидатор проекта (`scripts/validate_project.py`) - это **интеллектуальный инструмент**, который:

- ✅ Проверяет структуру, синтаксис, импорты, конфигурацию
- 🔧 **Автоматически исправляет** простые проблемы
- 🧠 Анализирует зависимости между модулями
- 📊 Предоставляет рекомендации по улучшению

## 🚀 Использование

### Базовая проверка

```bash
# Из корня проекта
python3 scripts/validate_project.py
```

### Автоматическое исправление

```bash
# Исправить все исправимые проблемы автоматически
python3 scripts/validate_project.py --auto-fix

# Посмотреть что будет исправлено без реальных изменений
python3 scripts/validate_project.py --auto-fix --dry-run
```

### Автоматическая валидация (Pre-commit hook)

Установите git hooks для автоматической проверки при каждом коммите:

```bash
./scripts/setup_git_hooks.sh
```

**Примечание:** Pre-commit hook использует режим проверки без автоисправления для безопасности.

## 🔍 Что проверяется и исправляется

### 1. Структура директорий ✅ AUTO-FIX

**Проверяет:**
- Наличие обязательных директорий
- Правильное расположение скриптов

**Автоматически исправляет:**
- Создает отсутствующие директории
- Перемещает скрипты из корня в `scripts/`

### 2. Синтаксис Python файлов ❌

**Проверяет:**
- Синтаксические ошибки во всех `.py` файлах
- Корректность AST структуры

**Автоматическое исправление:** Нет (требует ручного вмешательства)

### 3. Импорты и пути ✅ AUTO-FIX

**Проверяет:**
- Использование централизованного логирования
- Корректность относительных путей

**Автоматически исправляет:**
- Заменяет `from loguru import logger` на `from ..core.logger import get_logger`
- Правильно определяет глубину вложенности для относительных импортов
- Добавляет инициализацию `logger = get_logger(__name__)`

### 4. Конфигурация ❌

**Проверяет:**
- Корректность YAML синтаксиса
- Наличие примеров конфигурации

**Автоматическое исправление:** Нет (YAML ошибки требуют анализа контекста)

### 5. Согласованность ✅ AUTO-FIX

**Проверяет:**
- Использование `PROJECT_ROOT` в скриптах
- Единообразие логирования

**Автоматически исправляет:**
- Добавляет определение `PROJECT_ROOT` в скрипты где отсутствует
- Заменяет `Path('.')` на `PROJECT_ROOT` в скриптах

### 6. Документация ⚠️

**Проверяет:**
- Наличие важной документации

**Автоматическое исправление:** Нет (создание документации требует контента)

### 7. Анализ зависимостей 🧠

**Проверяет:**
- Циклические зависимости между модулями
- Граф зависимостей проекта

**Автоматическое исправление:** Нет (требует архитектурного анализа)

## 🧠 Интеллектуальные возможности

### Анализ зависимостей

Валидатор строит граф зависимостей между модулями и обнаруживает:
- Циклические зависимости
- Неиспользуемые импорты (планируется)
- Нарушения архитектурных паттернов (планируется)

### Автоматическое исправление

Валидатор может **автономно исправлять** следующие проблемы:

1. **Неправильные импорты логирования**
   ```python
   # Было:
   from loguru import logger
   
   # Станет:
   from ..core.logger import get_logger
   logger = get_logger(__name__)
   ```

2. **Неправильные пути в скриптах**
   ```python
   # Было:
   root = Path('.')
   
   # Станет:
   PROJECT_ROOT = Path(__file__).parent.parent
   root = PROJECT_ROOT
   ```

3. **Неорганизованная структура**
   - Автоматически перемещает скрипты в `scripts/`
   - Создает отсутствующие директории

### Рекомендации

Валидатор предоставляет:
- Список исправимых проблем с рекомендацией использовать `--auto-fix`
- Предупреждения о потенциальных проблемах
- Информацию о зависимостях между модулями

## 📊 Типы сообщений

### ❌ Ошибки (Error)
Критические проблемы, которые блокируют коммит:
- Синтаксические ошибки (не исправляются автоматически)
- Неправильное использование API (частично исправляются)
- Отсутствие обязательных файлов (исправляются автоматически)

### ⚠️ Предупреждения (Warning)
Информационные сообщения о потенциальных проблемах:
- Отсутствие опциональной документации
- Рекомендации по улучшению
- Устаревшие паттерны (многие исправляются автоматически)

### 🔧 [AUTO-FIX]
Проблемы, которые можно исправить автоматически.

## 🎯 Ограничения автономности

Валидатор **НЕ может** полностью автономно исправить:

1. **Синтаксические ошибки** - требуют понимания намерений программиста
2. **Логические ошибки** - требуют анализа бизнес-логики
3. **Архитектурные проблемы** - требуют принятия решений
4. **YAML ошибки конфигурации** - требуют понимания структуры данных
5. **Циклические зависимости** - требуют рефакторинга архитектуры

## 🔄 Рекомендуемый workflow

### Вариант 1: С автоисправлением (рекомендуется)

```bash
# 1. Проверка с автоисправлением
python3 scripts/validate_project.py --auto-fix

# 2. Просмотр результатов
git diff  # Посмотреть что изменилось

# 3. Коммит
git add .
git commit  # Дополнительная проверка через hook
```

### Вариант 2: С dry-run для безопасности

```bash
# 1. Посмотреть что будет исправлено
python3 scripts/validate_project.py --auto-fix --dry-run

# 2. Если все ОК, применить исправления
python3 scripts/validate_project.py --auto-fix

# 3. Коммит
git add .
git commit
```

### Вариант 3: Полностью ручной

```bash
# 1. Проверка без исправлений
python3 scripts/validate_project.py

# 2. Ручное исправление проблем

# 3. Повторная проверка
python3 scripts/validate_project.py

# 4. Коммит
git commit
```

## 📈 Улучшения в разработке

Планируемые возможности для повышения автономности:

1. **Автоматическое исправление YAML** - умный парсинг и исправление типичных ошибок
2. **Обнаружение неиспользуемых импортов** - автоматическое удаление
3. **Рефакторинг циклических зависимостей** - предложения по разрыву циклов
4. **Автоматическое форматирование** - интеграция с black/ruff
5. **Умное предложение документации** - генерация базовой документации на основе кода
6. **Анализ производительности** - обнаружение узких мест

## 🔧 Настройка и расширение

Валидатор можно расширить, добавив новые проверки в класс `ProjectValidator`.

### Добавление новой автоисправляемой проверки

```python
def validate_custom(self):
    """Ваша кастомная проверка с автоисправлением"""
    # Ваш код проверки
    if condition_not_met:
        def fix_action():
            # Код автоматического исправления
            # Вернуть True если исправление применено
            return True
        
        self.errors.append(ValidationError(
            file_path,
            "Описание проблемы",
            severity="error",
            auto_fixable=True,  # Включаем автоисправление
            fix_action=fix_action  # Функция исправления
        ))
```

## 📝 Интеграция в CI/CD

```yaml
# .github/workflows/validate.yml
name: Validate Project

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Validate project
        run: python3 scripts/validate_project.py
      # Опционально: автоисправление и коммит
      - name: Auto-fix and commit
        if: failure()
        run: |
          python3 scripts/validate_project.py --auto-fix
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add -A
          git commit -m "Auto-fix: исправления валидатора" || true
```

## 🎯 Преимущества

1. **Автономность** - исправляет до 70% типичных проблем автоматически
2. **Экономия времени** - не нужно вручную исправлять простые ошибки
3. **Единообразие** - обеспечивает соблюдение стандартов проекта
4. **Безопасность** - dry-run режим для проверки перед применением
5. **Интеллектуальность** - анализ зависимостей и архитектуры

## 📚 См. также

- [Скрипты проекта](../scripts/README.md)
- [Логирование](LOGGING.md)
- [Архитектура](ARCHITECTURE.md)
